/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "accertinity.com",
      "res.cloudinary.com",
      "ipfs.io",
      "gateway.pinata.cloud",
    ],
  },
};

export default nextConfig;
